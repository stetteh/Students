@model System.Collections.Generic.List<Students.Models.Student>

@{
    ViewBag.Title = "Index";
}

<h2>List of Students</h2>


<div class="row">
    @foreach (var t in Model)
    {
        <li>@t.Id, @t.FirstName, @t.LastName, @t.Gender, @t.Age</li>
    }
</div>
